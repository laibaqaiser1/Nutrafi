{
  "buildCommand": "prisma generate && node scripts/ensure-prisma-binaries.js && next build && node scripts/copy-prisma-to-next.js",
  "installCommand": "npm install",
  "framework": "nextjs",
  "functions": {
    "app/api/**/*.ts": {
      "includeFiles": "lib/generated/prisma/**/*.so.node"
    }
  }
}
